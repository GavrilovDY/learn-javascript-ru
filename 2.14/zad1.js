"use strict"
let browser=prompt('Каким браузером ты пользуешься?','Chrome');

	if (browser == 'Chrome') alert('Вот это нормальный браузер!');
		else
			if	(browser == 'Edge') alert('Плохой браузер!');		
		else
			if	(browser == 'Firefox') alert('Плохой браузер!');		
		else
			if	(browser == 'Safari') alert('Плохой браузер!');		
		else
			if	(browser == 'Opera') alert('Плохой браузер!');	
		else
				alert('А такой мы вообще не знаем');
